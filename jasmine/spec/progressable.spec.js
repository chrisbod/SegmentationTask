basicProgressableTests(Progressable,"Progressable");
/*describe("Generic progressible", function () {
it("should be able to provide a string type of it's data", function (done) {
      request.type().then(function(type){
        console.log(typeof type,JSON.stringify(type),request)
        expect(typeof type == "string").toBe(true)
        done()
      })
    },function () {
      debugger;
    });

})*/
